@inherits LayoutComponentBase
@inject Microsoft.Extensions.Configuration.IConfiguration Configuration

<div class="page">
    @{ var env = Configuration["Environment"]; }
    @if (!string.IsNullOrWhiteSpace(env) && !string.Equals(env, "PROD", StringComparison.OrdinalIgnoreCase))
    {
        <div class="dev-banner dev-banner-right">@env</div>
        <div class="dev-banner dev-banner-left">@env</div>
    }
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4">
            <AuthorizeView>
                <Authorized>
                    <div class="user-menu">
                        <div class="user-avatar" @onclick="ToggleUserMenu">
                            <span class="avatar-icon">👤</span>
                        </div>
                        @if (_showUserMenu)
                        {
                            <div class="user-dropdown">
                                <span class="dropdown-user-name">@context.User.Identity?.Name</span>
                                <a href="/.auth/logout?post_logout_redirect_uri=/" class="dropdown-item">Logout</a>
                            </div>
                        }
                    </div>
                </Authorized>
            </AuthorizeView>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>


@code {
    private bool _showUserMenu;

    private void ToggleUserMenu()
    {
        _showUserMenu = !_showUserMenu;
    }
}
