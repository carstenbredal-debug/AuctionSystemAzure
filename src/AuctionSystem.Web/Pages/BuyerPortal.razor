@page "/buyer"
@attribute [Authorize(Roles = "Admin,Buyer")]
@inject NavigationManager Nav

<PageTitle>Buyer Portal</PageTitle>

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("buyer/invitations", replace: true);
    }
}
