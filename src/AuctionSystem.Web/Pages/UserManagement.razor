@page "/admin/users"
@attribute [Authorize(Roles = "Admin")]
@inject AuctionApiClient Api

<PageTitle>Users & Roles - Admin</PageTitle>

<h1>Users & Roles</h1>
<p class="text-muted">Manage user access. Each user needs an Azure AD account and a role assignment to access the system.</p>

@if (_loading)
{
    <p><em>Loading...</em></p>
}
else
{
    @if (!string.IsNullOrEmpty(_message))
    {
        <div class="alert @(_messageIsError ? "alert-danger" : "alert-success") alert-dismissible fade show">
            @_message
            <button type="button" class="btn-close" @onclick="() => _message = null"></button>
        </div>
    }

    <div class="mb-3">
        <button class="btn btn-primary" @onclick="ShowAddForm">
            <i class="bi bi-plus-circle"></i> Add User
        </button>
    </div>

    @if (_showForm)
    {
        <div class="card mb-4 border-primary">
            <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                <strong>@(_editingUser?.Id > 0 ? "Edit User" : "Add New User")</strong>
                <button type="button" class="btn-close btn-close-white" @onclick="CancelForm"></button>
            </div>
            <div class="card-body">
                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label">Azure AD Object ID <span class="text-danger">*</span></label>
                        <input class="form-control" @bind="_editingUser!.AzureAdObjectId"
                               placeholder="e.g. 18a768cd-1d23-4942-92ca-2e1cc0d0955e" />
                        <small class="form-text text-muted">
                            Find in Entra ID → Users → select user → Object ID
                        </small>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Email <span class="text-danger">*</span></label>
                        <input class="form-control" type="email" @bind="_editingUser!.Email"
                               placeholder="user@infra-tech.dk" />
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Display Name <span class="text-danger">*</span></label>
                        <input class="form-control" @bind="_editingUser!.DisplayName"
                               placeholder="Full name" />
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Role <span class="text-danger">*</span></label>
                        <select class="form-select" @bind="SelectedRole">
                            <option value="">— Select Role —</option>
                            <option value="Admin">Admin — Full system access</option>
                            <option value="Broker">Broker — Bidding, allocations, customers</option>
                            <option value="Seller">Seller — Lot tracking, settlements</option>
                            <option value="Buyer">Buyer — Purchase history, allocations</option>
                        </select>
                    </div>
                </div>

                @if (_editingUser?.Role == "Broker")
                {
                    <hr class="my-4" />

                    <!-- General Section -->
                    <div class="card mb-3">
                        <div class="card-header bg-light"><strong>General</strong></div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-3">
                                    <label class="form-label fw-bold">No. <span class="text-danger">*</span></label>
                                    <input class="form-control" @bind="_brokerForm.BrokerNumber" />
                                </div>
                                <div class="col-md-5">
                                    <label class="form-label fw-bold">Name <span class="text-danger">*</span></label>
                                    <input class="form-control" @bind="_brokerForm.CompanyName" />
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label">Credit Limit</label>
                                    <input class="form-control" type="number" step="0.01" @bind="_brokerForm.CreditLimit" />
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label">Blocked</label>
                                    <select class="form-select" @bind="_brokerForm.Blocked">
                                        <option value="">— (None)</option>
                                        <option value="Ship">Ship</option>
                                        <option value="Invoice">Invoice</option>
                                        <option value="All">All</option>
                                    </select>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Address & Contact Section -->
                    <div class="card mb-3">
                        <div class="card-header bg-light"><strong>Address & Contact</strong></div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label">Address</label>
                                    <input class="form-control" @bind="_brokerForm.AddressLine1" />
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label">Address 2</label>
                                    <input class="form-control" @bind="_brokerForm.AddressLine2" />
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Country/Region Code</label>
                                    <select class="form-select" @bind="_brokerForm.Country">
                                        <option value="">Select Country...</option>
                                        @foreach (var c in _bcCountries)
                                        {
                                            <option value="@c.Code">@c.DisplayName (@c.Code)</option>
                                        }
                                    </select>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Post Code</label>
                                    <input class="form-control" @bind="_brokerForm.PostalCode" />
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">City</label>
                                    <input class="form-control" @bind="_brokerForm.City" />
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Contact Name</label>
                                    <input class="form-control" @bind="_brokerForm.ContactPerson" />
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Phone No.</label>
                                    <input class="form-control" @bind="_brokerForm.ContactPhone" />
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Mobile Phone No.</label>
                                    <input class="form-control" @bind="_brokerForm.MobilePhone" />
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">E-Mail</label>
                                    <input class="form-control" type="email" @bind="_brokerForm.ContactEmail" />
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Home Page</label>
                                    <input class="form-control" @bind="_brokerForm.HomePage" placeholder="https://" />
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Invoicing Section -->
                    <div class="card mb-3">
                        <div class="card-header bg-light"><strong>Invoicing</strong></div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-4">
                                    <label class="form-label">VAT Registration No.</label>
                                    <input class="form-control" @bind="_brokerForm.VatRegistrationNo" />
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Registration No.</label>
                                    <input class="form-control" @bind="_brokerForm.RegistrationNo" />
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Currency Code</label>
                                    <input class="form-control" @bind="_brokerForm.Currency" />
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Customer Group</label>
                                    <input class="form-control" @bind="_brokerForm.CustomerGroup" />
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Salesperson Code</label>
                                    <input class="form-control" @bind="_brokerForm.SalesPerson" />
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Language Code</label>
                                    <input class="form-control" @bind="_brokerForm.Language" />
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Payments Section -->
                    <div class="card mb-3">
                        <div class="card-header bg-light"><strong>Payments</strong></div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-4">
                                    <label class="form-label">Payment Terms Code</label>
                                    <input class="form-control" @bind="_brokerForm.PaymentTerm" />
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">Payment Method Code</label>
                                    <input class="form-control" @bind="_brokerForm.PaymentMethod" />
                                </div>
                                <div class="col-md-4 d-flex align-items-center pt-4">
                                    <div class="form-check form-switch">
                                        <input class="form-check-input" type="checkbox" @bind="_brokerForm.IsActive" />
                                        <label class="form-check-label">Active</label>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                }
                else if (_editingUser?.Role == "Seller")
                {
                    <hr class="my-4" />
                    <h5 class="mb-3">Seller Details</h5>
                    <div class="row g-3">
                        <div class="col-md-4">
                            <label class="form-label">Seller Number <span class="text-danger">*</span></label>
                            <input class="form-control" @bind="_sellerForm.SellerNumber" placeholder="S001" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Name <span class="text-danger">*</span></label>
                            <input class="form-control" @bind="_sellerForm.Name" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Name 2</label>
                            <input class="form-control" @bind="_sellerForm.Name2" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Search Name</label>
                            <input class="form-control" @bind="_sellerForm.SearchName" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Contact Name</label>
                            <input class="form-control" @bind="_sellerForm.ContactName" />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Address Line 1</label>
                            <input class="form-control" @bind="_sellerForm.AddressLine1" />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Address Line 2</label>
                            <input class="form-control" @bind="_sellerForm.AddressLine2" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Country</label>
                            <select class="form-select" @bind="_sellerForm.Country">
                                <option value="">Select Country...</option>
                                @foreach (var c in _bcCountries)
                                {
                                    <option value="@c.Code">@c.DisplayName (@c.Code)</option>
                                }
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Postal Code</label>
                            <input class="form-control" @bind="_sellerForm.PostalCode" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">City</label>
                            <input class="form-control" @bind="_sellerForm.City" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Phone No</label>
                            <input class="form-control" @bind="_sellerForm.ContactPhone" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Mobile Phone No</label>
                            <input class="form-control" @bind="_sellerForm.MobilePhone" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Email</label>
                            <input class="form-control" type="email" @bind="_sellerForm.ContactEmail" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Home Page</label>
                            <input class="form-control" @bind="_sellerForm.HomePage" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">VAT Registration No</label>
                            <input class="form-control" @bind="_sellerForm.VatRegistrationNo" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Registration No</label>
                            <input class="form-control" @bind="_sellerForm.RegistrationNo" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Customer Group</label>
                            <input class="form-control" @bind="_sellerForm.CustomerGroup" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Sales Person</label>
                            <input class="form-control" @bind="_sellerForm.SalesPerson" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Payment Term</label>
                            <input class="form-control" @bind="_sellerForm.PaymentTerm" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Payment Method</label>
                            <input class="form-control" @bind="_sellerForm.PaymentMethod" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Currency</label>
                            <input class="form-control" @bind="_sellerForm.Currency" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Language</label>
                            <input class="form-control" @bind="_sellerForm.Language" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Bank Name</label>
                            <input class="form-control" @bind="_sellerForm.BankName" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Bank Address</label>
                            <input class="form-control" @bind="_sellerForm.BankAddress" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Bank IBAN Number</label>
                            <input class="form-control" @bind="_sellerForm.BankIbanNumber" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">SWIFT Code</label>
                            <input class="form-control" @bind="_sellerForm.SwiftCode" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Bank Country</label>
                            <input class="form-control" @bind="_sellerForm.BankCountry" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Assignee</label>
                            <input class="form-control" @bind="_sellerForm.Assignee" />
                        </div>
                        <div class="col-md-4 d-flex align-items-center pt-4">
                            <div class="form-check form-switch me-4">
                                <input class="form-check-input" type="checkbox" @bind="_sellerForm.AssignmentOfReceivable" />
                                <label class="form-check-label">Assignment of Receivable</label>
                            </div>
                        </div>
                        <div class="col-md-4 d-flex align-items-center pt-4">
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox" @bind="_sellerForm.IsActive" />
                                <label class="form-check-label">Is Active</label>
                            </div>
                        </div>
                    </div>
                }
                else if (_editingUser?.Role == "Buyer")
                {
                    <hr class="my-4" />
                    <h5 class="mb-3">Buyer Details</h5>
                    <div class="row g-3">
                        <div class="col-md-4">
                            <label class="form-label">Buyer Number <span class="text-danger">*</span></label>
                            <input class="form-control" @bind="_buyerForm.BuyerNumber" placeholder="C001" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Name <span class="text-danger">*</span></label>
                            <input class="form-control" @bind="_buyerForm.Name" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Name 2</label>
                            <input class="form-control" @bind="_buyerForm.Name2" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Search Name</label>
                            <input class="form-control" @bind="_buyerForm.SearchName" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Contact Name</label>
                            <input class="form-control" @bind="_buyerForm.ContactName" />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Address Line 1</label>
                            <input class="form-control" @bind="_buyerForm.AddressLine1" />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Address Line 2</label>
                            <input class="form-control" @bind="_buyerForm.AddressLine2" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Country</label>
                            <select class="form-select" @bind="_buyerForm.Country">
                                <option value="">Select Country...</option>
                                @foreach (var c in _bcCountries)
                                {
                                    <option value="@c.Code">@c.DisplayName (@c.Code)</option>
                                }
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Postal Code</label>
                            <input class="form-control" @bind="_buyerForm.PostalCode" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">City</label>
                            <input class="form-control" @bind="_buyerForm.City" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Phone No</label>
                            <input class="form-control" @bind="_buyerForm.ContactPhone" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Mobile Phone No</label>
                            <input class="form-control" @bind="_buyerForm.MobilePhone" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Email</label>
                            <input class="form-control" type="email" @bind="_buyerForm.ContactEmail" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Home Page</label>
                            <input class="form-control" @bind="_buyerForm.HomePage" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">VAT Registration No</label>
                            <input class="form-control" @bind="_buyerForm.VatRegistrationNo" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Registration No</label>
                            <input class="form-control" @bind="_buyerForm.RegistrationNo" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Customer Group</label>
                            <input class="form-control" @bind="_buyerForm.CustomerGroup" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Sales Person</label>
                            <input class="form-control" @bind="_buyerForm.SalesPerson" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Payment Term</label>
                            <input class="form-control" @bind="_buyerForm.PaymentTerm" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Payment Method</label>
                            <input class="form-control" @bind="_buyerForm.PaymentMethod" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Currency</label>
                            <input class="form-control" @bind="_buyerForm.Currency" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Language</label>
                            <input class="form-control" @bind="_buyerForm.Language" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Bank Name</label>
                            <input class="form-control" @bind="_buyerForm.BankName" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Bank Address</label>
                            <input class="form-control" @bind="_buyerForm.BankAddress" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Bank IBAN Number</label>
                            <input class="form-control" @bind="_buyerForm.BankIbanNumber" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">SWIFT Code</label>
                            <input class="form-control" @bind="_buyerForm.SwiftCode" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Bank Country</label>
                            <input class="form-control" @bind="_buyerForm.BankCountry" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Assignee</label>
                            <input class="form-control" @bind="_buyerForm.Assignee" />
                        </div>
                        <div class="col-md-4 d-flex align-items-center pt-4">
                            <div class="form-check form-switch me-4">
                                <input class="form-check-input" type="checkbox" @bind="_buyerForm.AssignmentOfReceivable" />
                                <label class="form-check-label">Assignment of Receivable</label>
                            </div>
                        </div>
                        <div class="col-md-4 d-flex align-items-center pt-4">
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox" @bind="_buyerForm.IsActive" />
                                <label class="form-check-label">Is Active</label>
                            </div>
                        </div>
                    </div>
                }

                <div class="mt-4 text-end">
                    <button class="btn btn-primary me-2" @onclick="SaveUser">
                        @(_editingUser?.Id > 0 ? "Update User" : "Create User")
                    </button>
                    <button class="btn btn-outline-secondary" @onclick="CancelForm">Cancel</button>
                </div>
            </div>
        </div>
    }

    <div class="card">
        <div class="card-header">
            <strong>Registered Users</strong> <span class="badge bg-secondary">@_users.Count</span>
        </div>
        <div class="card-body p-0">
            <table class="table table-striped table-hover mb-0">
                <thead class="table-dark">
                    <tr>
                        <th>Display Name</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Linked Entity</th>
                        <th>Active</th>
                        <th style="width: 150px;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (_users.Count == 0)
                    {
                        <tr><td colspan="6" class="text-center text-muted py-4">No users registered. Click "Add User" to create the first user.</td></tr>
                    }
                    @foreach (var user in _users)
                    {
                        <tr>
                            <td><strong>@user.DisplayName</strong></td>
                            <td>@user.Email</td>
                            <td><span class="badge bg-@GetRoleBadgeColor(user.Role)">@user.Role</span></td>
                            <td>@GetLinkedEntity(user)</td>
                            <td>
                                @if (user.IsActive)
                                { <span class="badge bg-success">Active</span> }
                                else
                                { <span class="badge bg-secondary">Inactive</span> }
                            </td>
                            <td>
                                <button class="btn btn-sm btn-outline-primary me-1" @onclick="() => EditUser(user)">Edit</button>
                                <button class="btn btn-sm btn-outline-danger" @onclick="() => DeleteUser(user)">Delete</button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
}

@code {
    private List<AppUserDto> _users = new();
    private List<BrokerDto> _brokers = new();
    private List<SellerDto> _sellers = new();
    private List<BuyerDto> _buyers = new();
    private AppUserDto? _editingUser;
    private BrokerDto _brokerForm = new() { IsActive = true };
    private SellerDto _sellerForm = new();
    private BuyerDto _buyerForm = new();
    private bool _loading = true;
    private bool _showForm;
    private string? _message;
    private bool _messageIsError;
    private List<BcCountryRegionDto> _bcCountries = new();

    private string SelectedRole
    {
        get => _editingUser?.Role ?? "";
        set
        {
            if (_editingUser == null) return;
            _editingUser.Role = value;
            _editingUser.BrokerId = null;
            _editingUser.SellerId = null;
            _editingUser.BuyerId = null;
            _brokerForm = new BrokerDto
            {
                IsActive = true,
                ContactEmail = _editingUser.Email,
                ContactPerson = _editingUser.DisplayName,
                CompanyName = _editingUser.DisplayName
            };
            _sellerForm = new SellerDto
            {
                ContactEmail = _editingUser.Email,
                Name = _editingUser.DisplayName
            };
            _buyerForm = new BuyerDto
            {
                ContactEmail = _editingUser.Email,
                Name = _editingUser.DisplayName
            };
        }
    }

    protected override async Task OnInitializedAsync()
    {
        await LoadData();
        _loading = false;
    }

    private async Task LoadData()
    {
        _users = await Api.GetAllUsersAsync();
        _brokers = await Api.GetBrokersAsync();
        _sellers = await Api.GetSellersAsync();
        _buyers = await Api.GetAllBuyersAsync();
        _bcCountries = await Api.GetBcCountriesAsync();
    }

    private void ShowAddForm()
    {
        _editingUser = new AppUserDto { IsActive = true };
        _brokerForm = new BrokerDto { IsActive = true };
        _sellerForm = new SellerDto();
        _buyerForm = new BuyerDto();
        _showForm = true;
        _message = null;
    }

    private void EditUser(AppUserDto user)
    {
        _editingUser = new AppUserDto
        {
            Id = user.Id,
            AzureAdObjectId = user.AzureAdObjectId,
            Email = user.Email,
            DisplayName = user.DisplayName,
            Role = user.Role,
            BrokerId = user.BrokerId,
            SellerId = user.SellerId,
            BuyerId = user.BuyerId,
            IsActive = user.IsActive
        };
        _brokerForm = new BrokerDto { IsActive = true };
        _sellerForm = new SellerDto();
        _buyerForm = new BuyerDto();
        _showForm = true;
        _message = null;
    }

    private async Task SaveUser()
    {
        if (_editingUser == null) return;

        if (string.IsNullOrWhiteSpace(_editingUser.AzureAdObjectId) ||
            string.IsNullOrWhiteSpace(_editingUser.Email) ||
            string.IsNullOrWhiteSpace(_editingUser.DisplayName) ||
            string.IsNullOrWhiteSpace(_editingUser.Role))
        {
            _message = "Please fill in all required fields (Azure AD Object ID, Email, Display Name, Role).";
            _messageIsError = true;
            return;
        }

        // For new users, create the linked entity first
        if (_editingUser.Id == 0)
        {
            if (_editingUser.Role == "Broker")
            {
                if (string.IsNullOrWhiteSpace(_brokerForm.CompanyName) || string.IsNullOrWhiteSpace(_brokerForm.BrokerNumber))
                {
                    _message = "Broker Name and Broker Number are required.";
                    _messageIsError = true;
                    return;
                }
                var broker = await Api.CreateBrokerAsync(_brokerForm);
                if (broker == null)
                {
                    _message = "Failed to create broker.";
                    _messageIsError = true;
                    return;
                }
                _editingUser.BrokerId = broker.Id;
            }
            else if (_editingUser.Role == "Seller")
            {
                if (string.IsNullOrWhiteSpace(_sellerForm.SellerNumber) || string.IsNullOrWhiteSpace(_sellerForm.Name))
                {
                    _message = "Seller Number and Name are required.";
                    _messageIsError = true;
                    return;
                }
                var seller = await Api.CreateSellerAsync(_sellerForm);
                if (seller == null)
                {
                    _message = "Failed to create seller.";
                    _messageIsError = true;
                    return;
                }
                _editingUser.SellerId = seller.Id;
            }
            else if (_editingUser.Role == "Buyer")
            {
                if (string.IsNullOrWhiteSpace(_buyerForm.BuyerNumber) || string.IsNullOrWhiteSpace(_buyerForm.Name))
                {
                    _message = "Buyer Number and Name are required.";
                    _messageIsError = true;
                    return;
                }
                var buyer = await Api.CreateBuyerAsync(0, _buyerForm);
                if (buyer == null)
                {
                    _message = "Failed to create buyer.";
                    _messageIsError = true;
                    return;
                }
                _editingUser.BuyerId = buyer.Id;
            }
        }

        if (_editingUser.Id > 0)
        {
            var result = await Api.UpdateUserAsync(_editingUser.Id, _editingUser);
            if (result != null)
            {
                _message = $"User '{_editingUser.DisplayName}' updated successfully.";
                _messageIsError = false;
            }
            else
            {
                _message = "Failed to update user. Please check the details and try again.";
                _messageIsError = true;
                return;
            }
        }
        else
        {
            var result = await Api.CreateUserAsync(_editingUser);
            if (result != null)
            {
                _message = $"User '{_editingUser.DisplayName}' created with role '{_editingUser.Role}'.";
                _messageIsError = false;
            }
            else
            {
                _message = "Failed to create user. The Azure AD Object ID may already be in use.";
                _messageIsError = true;
                return;
            }
        }

        _showForm = false;
        _editingUser = null;
        await LoadData();
    }

    private void CancelForm()
    {
        _showForm = false;
        _editingUser = null;
    }

    private async Task DeleteUser(AppUserDto user)
    {
        await Api.DeleteUserAsync(user.Id);
        _message = $"User '{user.DisplayName}' deleted.";
        _messageIsError = false;
        await LoadData();
    }

    private string GetRoleBadgeColor(string role) => role switch
    {
        "Admin" => "danger",
        "Broker" => "primary",
        "Seller" => "success",
        "Buyer" => "info",
        _ => "secondary"
    };

    private string GetLinkedEntity(AppUserDto user)
    {
        if (user.BrokerId.HasValue)
            return _brokers.FirstOrDefault(b => b.Id == user.BrokerId.Value)?.CompanyName ?? $"Broker #{user.BrokerId}";
        if (user.SellerId.HasValue)
            return _sellers.FirstOrDefault(s => s.Id == user.SellerId.Value)?.Name ?? $"Seller #{user.SellerId}";
        if (user.BuyerId.HasValue)
            return _buyers.FirstOrDefault(b => b.Id == user.BuyerId.Value)?.Name ?? $"Buyer #{user.BuyerId}";
        return "—";
    }
}
